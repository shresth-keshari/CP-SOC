class Solution {
public:
    int search(vector<int>& nums, int target) {
        int found=0;
        for (int i=0; i<nums.size(); i++){
            if (nums[i]==target){
                found=1;
                return i;
            }
        }
        return -1;
    }
};
